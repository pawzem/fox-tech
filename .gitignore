# Jekyll build output & caches
_site/
.jekyll-cache/
.jekyll-metadata
.sass-cache/

# Ruby / Bundler  (Gemfile.lock IS committed — reproducible builds + Dependabot)
.bundle/
vendor/

# Node (only if front-end tooling is added later)
node_modules/

# Editor / OS cruft
.DS_Store
Thumbs.db
*.swp
*~
.idea/
.vscode/

# Claude Code local settings (personal, not for the public repo)
.claude/settings.local.json
